// form2_screen.dart
import 'package:flutter/material.dart';

class Form2Screen extends StatelessWidget {
  const Form2Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Form 2"),
      ),
      body: Center(
        child: Text("This is Form 2"),
      ),
    );
  }
}
